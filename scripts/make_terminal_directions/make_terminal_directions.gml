terminal_directions_map = ds_map_create();
ds_map_replace(terminal_directions_map,down,spr_terminal_down);
ds_map_replace(terminal_directions_map,left,spr_terminal_left);
ds_map_replace(terminal_directions_map,right,spr_terminal_right);